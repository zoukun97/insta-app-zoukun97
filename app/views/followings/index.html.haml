- content_for :header do
  .header_left
    = image_tag 'icon_back'
  .header_right
    Followings
.container
  - @users.each do |user|
    .follows
      .follow_user
        = image_tag user.avatar_image
      .follow_user_name 
        = user.account